# Shared configuration

Cross-application configuration schemas will live here when more than one TypeScript runtime consumes them. Secrets and environment-specific values do not belong in this package.
